name: "Test Workflow"
on:
  workflow_dispatch:
    inputs:
      lables:
        type: string
        required: false
        default: x64
      sleep:
        type: number
        required: false
        default: 20

jobs:
  doNothing1:
    name: "Do nothing"
    runs-on:
      group: Default2
      labels:
      - self-hosted
      - ${{ github.event.inputs.lables }}
    steps:
    - name: "Do nothing"
      run: |
        echo "Hello world"
        sleep ${{ github.event.inputs.sleep }}
  doNothing2:
    name: "Do nothing2"
    runs-on:
      - self-hosted
      - ${{ github.event.inputs.lables }}
    steps:
    - name: "Do nothing"
      run: |
        echo "Hello world"
        sleep ${{ github.event.inputs.sleep }}
  doNothing3:
    name: "Do nothin3"
    runs-on:
      - self-hosted
      - ${{ github.event.inputs.lables }}
    steps:
    - name: "Do nothing"
      run: |
        echo "Hello world"
        sleep ${{ github.event.inputs.sleep }}
  doNothing4:
    name: "Do nothin4"
    runs-on:
      - self-hosted
      - ${{ github.event.inputs.lables }}
    steps:
    - name: "Do nothing"
      run: |
        echo "Hello world"
        sleep ${{ github.event.inputs.sleep }}
  doNothing5:
    name: "Do nothin5"
    runs-on:
      - self-hosted
      - ${{ github.event.inputs.lables }}
    steps:
    - name: "Do nothing"
      run: |
        echo "Hello world"
        sleep ${{ github.event.inputs.sleep }}
  doNothing6:
    name: "Do nothing"
    runs-on:
      - self-hosted
      - ${{ github.event.inputs.lables }}
    steps:
    - name: "Do nothing"
      run: |
        echo "Hello world"
        sleep ${{ github.event.inputs.sleep }}
  doNothing7:
    name: "Do nothing2"
    runs-on:
      - self-hosted
      - ${{ github.event.inputs.lables }}
    steps:
    - name: "Do nothing"
      run: |
        echo "Hello world"
        sleep ${{ github.event.inputs.sleep }}
  doNothing8:
    name: "Do nothin3"
    runs-on:
      - self-hosted
      - ${{ github.event.inputs.lables }}
    steps:
    - name: "Do nothing"
      run: |
        echo "Hello world"
        sleep ${{ github.event.inputs.sleep }}
  doNothing9:
    name: "Do nothin4"
    runs-on:
      - self-hosted
      - ${{ github.event.inputs.lables }}
    steps:
    - name: "Do nothing"
      run: |
        echo "Hello world"
        sleep ${{ github.event.inputs.sleep }}
  doNothing10:
    name: "Do nothin5"
    runs-on:
      - self-hosted
      - ${{ github.event.inputs.lables }}
    steps:
    - name: "Do nothing"
      run: |
        echo "Hello world"
        sleep ${{ github.event.inputs.sleep }}
